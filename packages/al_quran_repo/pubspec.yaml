name: al_quran_repo
description: Repository layer for The Holy Qur'an app — wraps al_quran_api's data provider behind chapter, juz and bookmark repos.
version: 1.0.0
publish_to: none

environment:
  sdk: ^3.13.0

dependencies:
  al_quran_api:
    path: ../al_quran_api

dev_dependencies:
  lints: ^6.1.0
  test: ^1.26.0
